# Configuration file for the Sphinx documentation builder.
#
# This file only contains a selection of the most common options. For a full
# list see the documentation:
# https://www.sphinx-doc.org/en/master/usage/configuration.html

# -- Path setup --------------------------------------------------------------

# If extensions (or modules to document with autodoc) are in another directory,
# add these directories to sys.path here. If the directory is relative to the
# documentation root, use os.path.abspath to make it absolute, like shown here.

import datetime
from pathlib import Path

from sphinx.highlighting import lexers
from indurad_ci_latex_template.latex_style import get_latex_elements, Side

# see https://pygments.org/docs/lexers/
from pygments.lexers import TOMLLexer
from pygments.lexers import JsonLexer
import indurad_ci_sphinx_extensions.custom_md_parser  # type: ignore
import indurad_ci_sphinx_extensions.custom_rst_parser  # type: ignore

# -- Project information -----------------------------------------------------

project = "{{ project_name }}"
copyright = f"{datetime.datetime.now().strftime('%Y')}, indurad GmbH"
author = "{{ author }}"
master_doc = "{{ entry_point }}"
email = "{{ email }}"
document_date = "{{ document_date }}"

# -- General configuration ---------------------------------------------------

# Add any Sphinx extension module names here, as strings. They can be
# extensions coming with Sphinx (named 'sphinx.ext.*') or your custom
# ones.
# Add ReadTheDocs theme and CommonMark markdown parser
extensions = [
    "sphinx_autopackagesummary",
    "sphinx_rtd_theme",
    "sphinx.ext.todo",
    "sphinx.ext.graphviz",
    "sphinx.ext.intersphinx",
    "sphinx.ext.autodoc",
    "sphinx.ext.autosectionlabel",
    "sphinx.ext.autosummary",
    "sphinxcontrib.mermaid",
    "sphinxcontrib.plantuml",
    "sphinxcontrib.images",
    "sphinxcontrib.video",
    "ablog",
    "sphinx_git",
    "sphinx_markdown_builder",
    "indurad_ci_sphinx_extensions.custom_md_parser",
    "indurad_ci_sphinx_extensions.custom_rst_parser",
    "indurad_ci_sphinx_extensions.indurad_sphinx_extension",
]

# from https://stackoverflow.com/questions/54138944/how-to-set-up-default-settings-for-many-automodules
autodoc_default_options = {
    'members': True,
    'special-members': True,
    'exclude-members': '__dict__,__weakref__,__module__,__annotations__,__repr__,__str__', # exclude "standard" methods
    'undoc-members': True,
    'inherited-members': True,
    'show-inheritance': True,
}
autodoc_typehints_format = 'fully-qualified'

# Some auto-documented code causes sphinx issues such as
# "WARNING: py:class reference target not found: my_type"
#
# You may add a suppression file called 'sphinx_nitpick_exceptions'
# to your project root.
#
# Example format:
#
# # comment lines will be ignored
# py:class my_type
#
# See also:
# - https://documatt.com/blog/21/sphinx-conf-py-tips/#nitpick-ignore
# - https://www.sphinx-doc.org/en/master/usage/configuration.html#confval-nitpick_ignore
nitpick_ignore = []

sphinx_nitpick_ignore_config_file = Path("sphinx_nitpick_ignore")
if sphinx_nitpick_ignore_config_file.is_file():
    nitpick_ignore = [
        tuple(line.strip().split(None, 1))
        for line in sphinx_nitpick_ignore_config_file.read_text().split("\n")
        if not line.startswith("#")
    ]

autosectionlabel_prefix_document = True
autosectionlabel_maxdepth = 2

autosummary_generate = True

# https://github.com/sphinx-doc/sphinx/issues/12589
# Problem is claimed to be fixed, but appears to be not
suppress_warnings = [
    'autosummary.import_cycle',
]

source_parsers = {
    "md": indurad_ci_sphinx_extensions.custom_md_parser,
    "rst": indurad_ci_sphinx_extensions.custom_rst_parser,
}

lexers["toml"] = TOMLLexer()
lexers["jsn"] = JsonLexer()
lexers["json"] = JsonLexer()

# Add any paths that contain templates here, relative to this directory.
templates_path = ["_templates"]

# List of patterns, relative to source directory, that match files and
# directories to ignore when looking for source files.
# This pattern also affects html_static_path and html_extra_path.
exclude_patterns = list(
    {
        "node_modules",
        "_build",
        "Thumbs.db",
        ".DS_Store",
        {%- for exclude_pattern in exclude_patterns %}
        "{{ exclude_pattern }}",
        {%- endfor %}
    }
)

# A list of glob-style patterns that are used to find source files relative to the source directory.
# The default is **, meaning that all files are recursively included from the source directory.
include_patterns = ["**"]

# -- Options for HTML output -------------------------------------------------

# The theme to use for HTML and HTML Help pages.  See the documentation for
# a list of builtin themes.
html_theme = "sphinx_rtd_theme"

# Documentation for the 'Edit on GitLab' button:
# https://101.pages.indurad.x/pages/maintainer_notes.html#providing-full-urls
html_context = {
    "display_gitlab": True,  # integrate 'Edit on GitLab' function
    "gitlab_host": "git.indurad.x",
    "gitlab_user": "/",
    "gitlab_repo": "101/101.pages.indurad.x",
    "theme_vcs_pageview_mode": "edit",  # option is either "blob" or "edit"
    "gitlab_version": "master",
    "conf_py_path": "/",
}


# Add any paths that contain custom static files (such as style sheets) here,
# relative to this directory. They are copied after the builtin static files,
# so a file named "default.css" will overwrite the builtin "default.css".
html_static_path: list[str] = ["_static"]

# Disable footer line "Created by Sphinx"
html_show_sphinx = False

html_css_files = [
    'css/custom.css',
    'css/print.css'
]

# -- Extension options

numfig = True

numfig_format = {
    'figure': 'Figure %s',
    'table': 'Table %s',
}

todo_include_todos = True

mermaid_use_local = 'js/mermaid.js'
mermaid_cmd = "./mermaid.py"
mermaid_pdfcrop = "pdfcrop"

myst_heading_anchors = 5

myst_enable_extensions = [
    "amsmath",
    "deflist",
    "dollarmath",
    "linkify",
]

blog_baseurl = "https://101.pages.indurad.x"
blog_title = "Developer's Blog"

post_date_format = "%Y-%m-%d"

blog_post_pattern = "blog/posts/*.rst"

blog_authors = {
    "iMitarbeiter": ("iMitarbeiter", "https://indurad.com"),
}
blog_default_author = "iMitarbeiter"

version = "{{ document_version }}"

latex_engine = "lualatex"

latex_elements = get_latex_elements(
    email,
    version,
    "{{ watermark_text }}",
    {{ watermark_scale }},
    "{{ kicker }}",
    "{{ subheading }}",
    {{ logo_paths }},
    {{ insert_disclaimer }},
    "{{ chapterstart }}",
    Side("{{ even_page_num_pos }}"),
    Side("{{ odd_page_num_pos }}"),
)
latex_table_style = ['borderless', 'colorrows']

rst_epilog = f"""
.. |author| replace:: {author if author else "N/A"}
.. |version| replace:: {version if version else "N/A"}
.. |email| replace:: {email if email else "N/A"}
.. |document_date| replace:: {document_date if document_date else "N/A"}
"""

sphinx_build_config = {{ sphinx_build_config }}
