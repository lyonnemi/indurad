#### git revert OR git reset [technical]
- git revert: creates new commit that reverts changes to old version; history remains
- git reset: directly goes back to old commit; history changed

#### logging functionality [conceptual][technical]
- takes log messasge from code and sends them to various outputs ie console, files, networks etc
##### logger
- captures log messages from code
- filters messages based on severity level
- routes messages to handles
```python
import logging

logger = logging.getLogger(__name__) 
#creates logger instance that creates hierarchy in file
#used for consistent formatting, centralised configuration, aggregating logs from multiple files
#by declaring handlers we can determine where log messages go

console_handler = logging.StreamHandler() #sends to console
file_handler = logging.FileHandler("app.log") #sends to file named app.log
#second arguments
## mode='w' -> overwrite
## mode='a' -> append
file.setLevel(logging.ERROR) #added, so that file shows only ERROR level and above

logger.addHandler(file_handler) #added, so that any message declared in logger will now go to the file

#methods include severity level such as:
logger.debug()
logger.info()
logger.warning()
logger.error() #problem that prevents function
logger.critical() #serious error
logger.exception() #like error() buit includes exception traceback
logger.log() #custom logging level

logger.setLevel(logging.Warning) #filters message to only this warning or above; anything below filtered out
logger.
```

#### logging onto console [technical]
```python
import logging

logger = logging.getLogger("logger name")
console_handler = console.StreamHandler()
logger.addHandler(console_handler)
```

#### Definition of Jobs in CICD [technical]
- execution of specific tasks ie Test, Build, Deploy

#### CICD additionals [technical][conceptual]
- script : codes to run on terminal


#### configuring yaml files [technical]
- declare stages with equivalent stages
- come outh with link and stage: one of the declared stages

#### displaying the link to the job log 
```yaml
job_name: 
   script :
	echo "the build log can be found at: $CI_JOB_URL

#### assertRegex vs assertEqual [technical]
- assertEqual() : exact match line for line
- assertRegex() : check for a pattern match
